# GitHub 上传报告

生成时间：2026-05-31 12:55:00

## 一、上传仓库

系统目录已上传至：

```text
https://github.com/zekai00/InkLandscapeGen
```

本地目录：

```text
/root/Workspace/ControllableImageGeneration/system
```

## 二、上传前处理

上传前完成了以下清理：

- 将 `backend/app/glm4.py` 中硬编码的智谱 API Key 改为从 `ZHIPUAI_API_KEY` 环境变量读取。
- 将 `backend/app/translator.py` 中硬编码的百度翻译 App ID 和 API Key 改为从 `BAIDU_TRANSLATE_APP_ID`、`BAIDU_TRANSLATE_API_KEY` 环境变量读取。
- `.env`、`node_modules`、`dist`、`__pycache__`、旧版 legacy 代码、备份文件和临时日志均未提交。

## 三、验证结果

已完成以下验证：

```bash
python -m compileall backend/app
cd frontend && npm run build
```

结果：

- 后端 Python 文件语法检查通过。
- 前端生产构建通过。
- 前端构建仍有资源体积和 Browserslist 提示，不影响运行。

## 四、提交信息

首次提交：

```text
88a1eb4 Initial system implementation
```

后续 README 和报告同步提交后，以远程 `main` 分支为准。
